{
  "name": "@datell/mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal stdio MCP server for Datell visual report rendering.",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "datell-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/index.js",
    "smoke": "npm run build && node ./scripts/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0-alpha.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0-alpha.2",
    "@cfworker/json-schema": "^4.1.1",
    "@types/node": "^22.15.30",
    "typescript": "^5.9.3"
  }
}